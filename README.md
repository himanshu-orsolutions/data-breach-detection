# data-breach-detection
This application detects the data breaches which can happen while remote file transfers. It closely observes all the file operations going on the system and alerts the user if any unidentified path is detected.
